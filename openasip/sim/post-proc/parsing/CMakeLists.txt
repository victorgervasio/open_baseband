set(GEN_DATASET_PY
    "${CMAKE_CURRENT_SOURCE_DIR}/gen_dataset.py"
)

set(FIND_LATEST_LOG_CMAKE
    "${CMAKE_CURRENT_SOURCE_DIR}/find_latest_log.cmake"
)

set(DATASET_TARGET "dataset-${OPENASIP_TARGET}")

add_custom_target(${DATASET_TARGET}
    COMMAND
        "${CMAKE_COMMAND}"
        -DSIM_OUTPUT_DIR="${SIM_OUTPUT_DIR}"
        -DDATASET_LOG_DIR="${DATASET_LOG_DIR}"
        -DPYTHON_EXECUTABLE="${Python3_EXECUTABLE}"
        -DGEN_DATASET_PY="${GEN_DATASET_PY}"
        -P "${FIND_LATEST_LOG_CMAKE}"
    WORKING_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}"
    USES_TERMINAL
)
